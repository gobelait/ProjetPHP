CREATE TABLE produit (
      id INTEGER,
      sexe STRING,
      nom STRING,
      codetype INTEGER,
      tailles STRING,
      prix FLOAT,
      description STRING,
      couleurs STRING
      );

CREATE TABLE categorie (
      code INTEGER,
      nom STRING,
      sexe STRING
      );
