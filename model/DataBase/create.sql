CREATE TABLE produit (
      id INTEGER,
      sexe STRING,
      nom STRING,
      codetype INTEGER,
      prix FLOAT,
      description STRING,
      couleurs STRING
      );

CREATE TABLE categorie (
      code INTEGER,
      nom STRING,
      sexe STRING
      );

CREATE TABLE admin (
      id STRING,
      mdp STRING
);
